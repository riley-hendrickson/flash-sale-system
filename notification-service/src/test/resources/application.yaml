spring:
  application:
    name: notification-service
  config:
    # "optional:" so tests don't hard-fail when no config server is reachable
    import: "optional:configserver:${CONFIG_SERVER_URL:http://localhost:8888}"
  kafka:
    bootstrap-servers: ${KAFKA_BOOTSTRAP_SERVERS:localhost:9092}
    consumer:
      group-id: order-notifications
      auto-offset-reset: earliest
      key-deserializer: org.apache.kafka.common.serialization.StringDeserializer
      value-deserializer: org.springframework.kafka.support.serializer.JacksonJsonDeserializer
      properties:
        spring.json.trusted.packages: "flashsalesystem.notificationservice.events"
        spring.json.value.default.type: "flashsalesystem.notificationservice.events.OrderEvent"

server:
  port: 8083
